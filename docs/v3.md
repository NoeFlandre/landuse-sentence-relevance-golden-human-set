# V3 workflow (candidate pool shipped, annotation seed shipped; resumable annotation UI; publication paused)

V3 extends the human set from the 154 adjudicated V2 rows to a 300-row state
across three sources. The streaming adapters and issue #6's oversized candidate
pool/geographic preflight, plus issue #7's deterministic annotation seed, are
implemented; the resumable annotation UI is available when explicitly selected,
while the default command remains the V2 pipeline. The V3 seed contains frozen
V2 rows plus unlabeled fresh slots, and its benchmark remains
`data/benchmark/v2-adjudicated.csv`.

This page records the implemented adapter, candidate-pool, annotation-seed, and
resumable UI contracts. V3 publication remains tracked in
[#10](https://github.com/NoeFlandre/landuse-sentence-relevance-golden-human-set/issues/10).
The candidate pool and geographic preflight are implemented alongside the
resumable UI.
For the V2 workflow, see
[Data sources and models](data-sources.md) and [Annotation](annotation.md).

## What exists today

| Aspect | Implemented V2 behavior | Implemented V3 / remaining design |
| --- | --- | --- |
| Sources | Two: Wikipedia sections and website-tag text | Three: description tag, Wikipedia sentences, website-tag sentences; streaming adapters shipped in issue #5 |
| Sentence splitting | SaT-12L-sm runs locally | No local splitter in the adapters; upstream sentence records only |
| Language filtering | CommonLingua runs locally on website text | No local language model in the adapters; upstream language metadata only |
| Settings | `Settings` in `src/landuse_sentence_relevance/config.py` pins the two V2 dataset revisions and the V2 model revisions | **Shipped (issue #4):** `V3Settings` in the same module pins the three V3 dataset revisions; it needs no model revisions |
| Paths | `results/candidates/v2/…`, `results/annotations/sessions/v2-wikipedia.jsonl` | **Shipped (issue #4):** separate V3 paths on `V3Settings`, anchored to the project data root |
| Rows | 154 adjudicated rows, two sources | 300 seed rows, 100 per source, 50 target Yes / 50 target No per source |
| Candidate pool | 512 reusable candidates, 256 per source | **Shipped (issue #6):** at least 400 fresh, globally unique H3 resolution-3 cells per source, with every V2 cell reserved |
| Annotation seed and UI | V2 annotation session only | **Shipped (issues #7 and #8):** 99 Wikipedia and 54 website V2 rows reused; 147 fresh rows selected deterministically, left unlabeled, and exposed through the resumable V3 UI |

## Planned upstream datasets

The V3 adapters stream three public Hugging Face datasets and, as in V2, pin
each revision by commit SHA and never materialize a dataset locally.
`V3Settings` holds those pins, and they are deliberately not
environment-overridable.

| Source | Dataset | Pinned revision | Configuration/split the settings name |
| --- | --- | --- | --- |
| Description tag | [OSM polygon description tag](https://huggingface.co/datasets/NoeFlandre/osm-polygon-description-tag) | `fec858b679f5ee7e87f0ecfaaa6b7223b2a7f5e2` | `language-v1`/`train` sentence records with `language_code` and `sentences`, joined to `default`/`train` for geometry |
| Wikipedia | [OSM polygon Wikidata and Wikipedia](https://huggingface.co/datasets/NoeFlandre/osm-polygon-wikidata-and-wikipedia) | `f48c5aaaec6aecd63ecf5c195565cc2787597f1e` | `wikipedia_sentences`/`wikipedia_sentences` records with `language`, `project`, and `section_index` |
| Website tag | [OSM polygon website tag](https://huggingface.co/datasets/NoeFlandre/osm-polygon-website-tag) | `5c8e56a50b5679118a28aef057af002209f80a5e` | `default`/`polygons`, whose rows already carry the pre-split sentence columns and their `*_language` fields |

The website dataset publishes no separate sentence configuration: its
pre-split sentences are columns inside `default`/`polygons`, so the settings
read that split rather than a sentence-level one. The description dataset's
`language-v1` records carry no coordinates, which is why geometry is resolved
by the join described below.

### Planned sentence and language contract

All three upstream datasets already publish sentence-level records and language
metadata, so the V3 adapters consume them as they are:

- Sentences come from the upstream sentence fields. **No local splitter runs**:
  SaT-12L-sm is used by V2 today and is not used by the V3 adapters.
- English filtering uses upstream language metadata only. **No local language
  model runs**: CommonLingua is used by V2 today and is not used by the V3
  adapters.
- Wikipedia rows are restricted to `project=wikipedia` and English articles.
  **Wikivoyage is excluded**: the `wikivoyage_sentences` and
  `wikivoyage_sections` configurations are never read, matching the V2 rule
  that already excludes Wikivoyage.
- Wikipedia selection stays contextual: title-only and lead-only sentences are
  rejected, as in V2. The pinned revision publishes no `is_lead` or `is_title`
  column, so the rule is built on the columns it does publish and on what they
  hold in the recorded schema fixture:
    - the **lead** is `section_index` 0. Every lead row in the recorded shard
      also carries an empty `heading` at `level` 0 with an empty `section_path`,
      and no body row has an empty heading, so an empty heading is read as the
      lead too.
    - a **section title** reaches the sentence table as the first sentence of a
      body section, rendered with the MediaWiki edit marker (`[ edit ]` or
      `[ edit | edit source ]`). In the recorded shard all 592 body-section
      first sentences start with that marker and it appears nowhere else, so
      rejecting those rows drops heading lines only.
    - `sentence_index` alone never decides this: a body sentence that is not a
      heading line is kept whatever its index.
- Where sentence records carry no coordinates, geolocation is resolved with
  bounded streaming joins back to the polygon records, keeping provenance
  deterministic. The join reads the polygon or geometry side once across all of
  its shards and keeps only a small projection of each row — identity,
  coordinates, place name, region, and source URL — so a sentence still finds
  its polygon when the two rows fall in differently numbered shards. Both axes
  stay bounded: `max_rows_per_shard` rows per shard, and at most
  `max_join_entries` keys held at once.
- Provenance travels with every candidate, from the columns each pinned
  revision actually publishes:
    - `region` comes from the `region` column of the Wikipedia and website
      polygons. The description dataset publishes no `region`, so it is derived
      from `source_pbf` by stripping the extract suffixes
      (`afghanistan-latest.osm.pbf` → `afghanistan`). The sibling datasets of
      the same extract publish exactly that label, so the fallback reproduces
      the published value rather than inventing one.
    - `source_url` is the `osm_url` of a description geometry, the `website` or
      `contact_website` of a website row, and, for Wikipedia, a URL derived from
      `page_id` (`https://en.wikipedia.org/?curid=…`) because the sentence table
      publishes no URL column.

### Projections are checked against the recorded upstream schemas

`tests/fixtures/v3_upstream_schema.json` records the Parquet schema of all five
pinned streams, alongside the sampled facts the Wikipedia and region rules rest
on. The unit tests assert every projected column against that file, so a
projection can never name a column a pinned revision does not publish. Re-record
it with `uv run python scripts/record_v3_schema.py` when a pin changes.

`scripts/streaming_smoke.py` is the network gate: it opens all five V3 streams
with the exact production projections at the pinned revisions, reports any
projected column the stream does not return, and then builds a few real
candidates per source from one pinned shard each.

## Candidate pool and geographic preflight

Run the builder from the project checkout:

```bash
./scripts/uv-seagate run python scripts/build_v3_candidate_pool.py
```

The builder streams the five pinned upstream inputs through the V3 adapters,
reserves every H3 cell in `data/benchmark/v2-adjudicated.csv`, and retains a
deterministically selected candidate per globally unique H3 resolution-3 cell.
It finalizes at least 400 cells per source by default, then fails before any
annotation workflow can start if the source reservoirs or V2-seeded quota
shortfalls are insufficient. Its stdout is a compact JSON preflight summary;
it never prints candidate sentences or raw upstream rows.

The only V3 working artifacts are written under the `V3Settings` data root on
the Seagate project drive:

- `results/candidates/v3/progress.json` — an atomic, resumable checkpoint of
  compact candidate metadata; it is written during collection and after an
  interrupted stream.
- `results/candidates/v3/pool.json` — the finalized compact reservoir and its
  metadata, written only after geographic and quota preflight succeeds.

The upstream rows are streamed and discarded. The V2 benchmark stays committed
and immutable; its rows are used only to derive the seed plan and reserved-cell
set.

## V2 reuse and quota arithmetic

`data/benchmark/v2-adjudicated.csv` is immutable and is never edited. V3 seeds
its state from that file and exposes only the remaining rows for future human
annotation.

| Source | Seeded from V2 | Fresh unlabeled slots | V3 target total (yes / no) |
| --- | --- | --- | --- |
| Wikipedia | 99 | 1 | 100 (50 / 50) |
| Website | 54 | 46 | 100 (50 / 50) |
| Description | 0 | 100 | 100 (50 / 50) |
| **Total** | **153** | **147** | **300** |

V2 holds 100 Wikipedia rows (49 yes, 51 no) and 54 website rows (31 yes, 23 no).
V3 reuses 99 Wikipedia rows — one surplus `no` row is left out of the V3 quota
— and adds a single Wikipedia `yes` row to reach 50 / 50. All 54 website rows
are reused, so 46 fresh website rows close the per-label gap, and the
description source contributes 100 entirely new rows. Seeded V2 rows count
toward progress metrics but are never presented for reannotation; pending
rows carry no human label.

## Annotation seed and resume behavior

After the issue #6 reservoir has been built, run the seed builder from the
project checkout:

```bash
./scripts/uv-seagate run python scripts/build_v3_annotation_seed.py
```

The builder loads the frozen V2 benchmark read-only, validates the existing
candidate-pool preflight, and writes
`results/annotations/seeds/v3.json`. Its metadata records the benchmark SHA-256,
candidate-pool digest, quota matrix, and selection policy. The output is a
compact JSON summary containing counts and the excluded V2 row's deterministic
reason; it never prints candidate sentences or invents labels.

If the process is interrupted before the atomic write, rerunning it selects the
same source-by-label slots. If the seed exists, matching metadata causes it to
be reused without reopening upstream streams or selecting new rows. A changed
benchmark, candidate pool, seed, or quota matrix is rejected rather than
silently mixing states.

## Resumable annotation UI

Run the V3 UI explicitly from the project checkout:

```bash
ANNOTATION_VERSION=v3 ./scripts/uv-seagate run landuse-annotate
```

The entrypoint builds the V3 candidate pool and seed first. It starts only after
candidate preflight succeeds; a failed preflight is raised before a web server
or V3 session is created. The default command without `ANNOTATION_VERSION=v3`
continues to run V2.

The page shows total progress for all 300 rows, per-source progress for
Wikipedia, website, and description, Yes/No counts against their targets, and
each remaining source-by-label quota. Seeded V2 rows are counted in those
metrics, but seeded v2 rows are never shown for reannotation. Only the 147
fresh V3 rows appear as current candidates or editable review records.

Every label, relabel, and removal replaces the V3 JSONL session through atomic
persistence. Reloading the process validates the saved candidate content
against the immutable V3 seed and resumes at the next unlabeled fresh row.
The V3 workflow has no publisher: completing the local session never uploads
or publishes V3 data.

## Storage paths

V3 keeps its own files beside the V2 ones on the Seagate project drive,
mirroring the V2 layout under a `v3` prefix rather than reusing any V2 path.
`V3Settings` defines the V3 paths below. Issue #6 writes the candidate pool and
progress checkpoint; issue #7 writes the annotation seed; human annotation
and issue #8 writes the V3 session through the UI.

| Artifact | `Settings` (V2) path | `V3Settings` path |
| --- | --- | --- |
| Candidate pool | `results/candidates/v2/pool.json` | `results/candidates/v3/pool.json` |
| Candidate progress checkpoint | `results/candidates/v2/progress.json` | `results/candidates/v3/progress.json` |
| Annotation seed | — | `results/annotations/seeds/v3.json` |
| Annotation session | `results/annotations/sessions/v2-wikipedia.jsonl` | `results/annotations/sessions/v3.jsonl` |
| Runtime and auth state | `state/runtime-cache`, `state/huggingface-auth` | unchanged |

No V3 path overwrites V2 state. Candidate building checkpoints compact
metadata atomically and can resume from an interrupted source stream. The seed
is written atomically and can resume from a matching state; it does not contain
human labels for fresh rows. The UI session is a separate atomic V3 JSONL file
and does not modify `data/benchmark/v2-adjudicated.csv`.

## Limitations

- The V3 UI is local-only. There is no published V3 split or `v3` benchmark
  file under `data/benchmark/`; publication remains a separate issue.
- The publication behavior in issue #10 remains design intent and may change
  before release.
- V3 changes nothing about V2: the released `v2` artifacts, splits, and tags are
  frozen, and the [versioning policy](versioning.md) still applies.
- The design inherits upstream sentence segmentation and language labels, so
  upstream segmentation or language errors would not be corrected locally.
- Coverage would be one sentence per H3 resolution-3 cell, which bounds
  geographic density rather than guaranteeing balanced regional representation.
- The description source is new in V3 and has no V2 rows to reuse, so it would
  depend entirely on fresh annotation.
